# === WEHTTAMSNAPS CONFIGURATION OVERRIDES ===
# GitHub: https://github.com/Crowdrocker
# Custom overrides and experimental features

# User-specific overrides should go here
# This file is last to override any previous settings

# Experimental features (uncomment to enable)
# experimental:features = 
# experimental:send_damage_by_region = true
# experimental:direct_scanout = true

# Custom debugging
# debug:disable_logs = false
# debug:enable_stdout_logs = true
# debug:damage_blink = false

# Performance overrides (adjust as needed)
# misc:vfr = true
# misc:vrr = 0
# misc:no_direct_scanout = false

# Custom animations (experimental)
# bezier = customBezier, 0.42, 0, 0.58, 1
# animation = windows, 1, 3, customBezier, popin

# Monitor-specific overrides
# monitor = desc:Your Monitor Description, addreserved, 0, 0, 0, 0

# Application-specific overrides
# windowrule = float, ^(custom-app)$
# windowrule = size 800 600, ^(custom-app)$

# Keybinding overrides
# bind = $mod, CUSTOM, exec, custom-command

# Environment variable overrides
# env = CUSTOM_VAR = custom_value

# Theme overrides
# decoration:col.active_border = rgba(cust000ff)
# decoration:col.inactive_border = rgba(cust111ff)

# Layout overrides
# dwindle:preserve_split = true
# master:new_is_master = false

# Workspace overrides
# workspace = 1,monitor:desc:Specific Monitor

# Performance tweaks
# renderer:direct_scanout = true
# damage_tracking = full

# User customization section starts here
# Add your personal customizations below this line

# Example: Custom wallpaper per workspace
# workspace = 1, swww img ~/Pictures/wallpapers/workspace1.jpg
# workspace = 2, swww img ~/Pictures/wallpapers/workspace2.jpg

# Example: Custom keybindings
# bind = $mod SHIFT, P, exec, ~/custom-script.sh

# Example: Custom window rules
# windowrulev2 = opacity 0.8,class:^(custom-app)$

# Example: Custom animations
# animation = workspaces, 1, 7, slidefade

# End of user customization section

# WehttamSnaps - Professional Arch Linux Hyprland Setup
# Last modified: $(date)
# Config version: 1.0.0